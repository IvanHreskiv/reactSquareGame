pipeline:
  frontend:
    image: node
    commands:
      - npm i
      - npm test

  publish-api-docker:
    image: plugins/docker
    repo: ihreskiv/squaregame
    tags: latest
    secrets: [dockerhub_username, dockerhub_password]
    volumes:
     - /var/run/dockerr.sock:/var/run/docker.sock
