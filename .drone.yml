pipeline:
  frontend:
    image: node
    commands:
      - npm i
      - npm test

  publish-api-docker:
    image: plugins/docker:1.12
    repo: ihreskiv/squaregame
    tags: latest
    secrets: [dockerhub_username, dockerhub_password]
