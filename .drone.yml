pipeline:
  frontend:
    image: node
    commands:
      - npm i
      - npm run server

services:
  postgres:
    image: postgres
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_DB=test